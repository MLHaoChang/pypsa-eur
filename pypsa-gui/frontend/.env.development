# Loaded automatically by Vite in development.
# Forces the multi-user auth UI (login / projects / admin) for local review.
# To run the classic single-user workbench instead, create `.env.local` with:
#   VITE_AUTH_ENABLED=false
VITE_AUTH_ENABLED=true
